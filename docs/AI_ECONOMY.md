# Экономика ИИ в CK3 — сводный разбор (как ИИ тратит золото)

Один файл со всеми функциями, значениями и пошаговым примером. Источники —
`vanilla/00_ai.txt`, `vanilla/00_defines.txt`, `vanilla/scripted_values/00_ai_values.txt`,
`.../00_men_at_arms_values.txt`, `.../00_suggestion_values.txt`.

---

## 0. Базовый принцип: кошелёк ОДИН

У ИИ одна казна (одно число золота) и один помесячный баланс. «Военная казна /
развитие / война-сейчас» — это **не сундуки**, а мысленные ярлыки, которыми ИИ
помечает части одной кучи, чтобы решить, что сейчас можно тратить, а что
«отложено». Плюс есть **два типа расходов**:
- **Разовые** (из кучи): найм латников, постройка здания, найм наёмников.
- **Постоянные** (вычитаются из дохода каждый месяц): содержание латников и
  поднятых левий.

---

## 1. Все значения и функции экономики

### 1.1 Военная казна (резерв на войну) — `00_ai.txt`
| Ключ | Значение | Смысл |
|---|---|---|
| `MIN_WAR_CHEST` | `{25 25 50 100 200 300 400}` | пол казны по ярусам (граф = **50**) |
| `MONTHS_OF_MAINTENANCE_IN_WAR_CHEST` | `18` | цель = 18 мес. содержания армии |
| `PERCENTAGE_INTO_WAR_CHEST` | `0.6` | пока казна не полна — 60% дохода в неё |

**Цель казны = `max(MIN_WAR_CHEST[ярус]; 18 × месячное содержание армии)`.**
У графа без большой армии 18×содержание мало → побеждает пол **50**.

Параллельные скриптовые значения — `00_ai_values.txt`:
| Функция | Значение |
|---|---|
| `cautious_ai_minimum_war_chest_gold` | `50` (+100 герцог / +200 король / +300 имп.) |
| `conqueror_safe_spending_gold` | `2 × максимум казны` (завоеватель копит вдвое) |

### 1.2 Дележ свободного золота — `00_ai.txt`
| Ключ | Значение | Смысл |
|---|---|---|
| `BUDGET_CATEGORY` | Развитие `0.20` / Война-сейчас `0.80` | как делится остаток после казны |
| `BUDGET_CATEGORY_MAX` | `5000` на категорию | потолок; излишек переливается |
| `BUDGET_CATEGORY_SHORT_TERM_MIN` | `{25 25 200 200 400 400 400}` | подушка ликвидности (граф = **200**) |

- **Развитие (LongTerm, 20%)** — постройки, покупка новых латников, проекты.
- **Война-сейчас (ShortTerm, 80%)** — наёмники, срочные военные нужды.
- **Подушка 200** — ИИ держит ~200 свободных «на случай войны прямо сейчас»
  (быстро нанять наёмников); ниже неё неохотно вкладывает в долгое.

### 1.3 Содержание латников как доля ДОХОДА — `00_ai_values.txt`
| Функция | База | Смысл |
|---|---|---|
| `ai_men_at_arms_expense_gold_min` | `0.15` | быстро довести расход на латников до 15% дохода |
| `ai_men_at_arms_expense_gold_ideal` | `0.40` | цель 40% дохода; выше **не покупает** |
| `ai_men_at_arms_expense_gold_max` | `0.60` | выше 60% дохода → **РАСПУСКАЕТ латников** |

Модификаторы: воин +0.1, завоеватель +0.2, строитель −0.1…−0.2, директива
вассалу «строить латников» +0.5; админ/кочевые при малом долге → `max = 3`.

### 1.4 Сколько латников держать — `00_suggestion_values.txt` + `00_defines.txt`
| Ключ/функция | Значение | Смысл |
|---|---|---|
| `recommended_num_maa_regiments` | `ярус − 1` | граф 1, герцог 2, король 3, имп. 4 |
| `REGIMENT_MAA_CAP_BY_TIER` | `{0 1 2 3 4 5 6}` | лимит полков (граф = **2**, сверх — штраф) |
| `REGIMENT_MAA_EXTRA_BY_CAP` | `0.2` | штраф за полк сверх лимита |
| `REGIMENT_DEFAULT_STACK_SIZE` | `100` | бойцов в одном отряде |
| `REGIMENT_DEFAULT_MAX_SIZE` | `3` | базово отрядов в полку → **300 бойцов** |
| `REGIMENT_MAX_SIZE` | `1000` | потолок полка = **10 отрядов** (поздняя игра) |

### 1.5 Цены латников — `00_men_at_arms_values.txt` (найм / содержание мирн.)
| Тип | Найм | Содержание/мес (мирн.) | в войне (×3) |
|---|---|---|---|
| Застрельщики | 45 | 0.15 | 0.45 |
| Лучники | 55 | 0.20 | 0.60 |
| Копейщики | 75 | 0.30 | 0.90 |
| Тяжёлая пехота | 90 | 0.40 | 1.20 |
| Лёгкая конница | 85 | 0.35 | 1.05 |
| Тяжёлая конница | 200 | 0.70 | 2.10 |

`@high_maint_mult = 3` — в поднятом (военном) состоянии содержание ×3.

### 1.6 Содержание солдат и левий — `00_defines.txt`
| Ключ | Значение | Смысл |
|---|---|---|
| `GOLD_COST_PER_SOLDIER` | `0.003` | золота за **каждого** солдата/мес |
| `RAID_ARMY_COST_MULT` | `0.5` | рейд-армии содержатся вдвое дешевле |

**Про левии (важно, по твоему вопросу):**
- **Поднять левии — бесплатно** (нет цены найма).
- **Но поднятые левии стоят содержания**: `0.003 × число бойцов / мес`.
  - 5000 поднятых левий = `5000 × 0.003 = 15 золота/мес`, пока подняты.
- **Не подняты (сидят дома) — стоят 0.**

То есть мой прежний тезис «левии не стоят денег» верен **только для мира**
(не подняты). В войну поднятые левии — реальный расход, который вместе с
наёмниками и съедает бюджет.

### 1.7 Роспуск устаревших — `00_ai.txt`
| Ключ | Значение | Смысл |
|---|---|---|
| `REGIMENT_OBSOLETION_SCORE_DIFFERENCE` | `20` | распустить полк, если на 20 хуже лучшего И денег нет |

---

## 2. Куда что идёт (приоритеты траты)

Водопад (упрощённая модель реальной логики):
1. **Военная казна** — держать пол (граф 50), не трогать кроме войны.
2. **Латники** — добрать до рекомендованного числа (военный приоритет, из
   «Война-сейчас»).
3. **Подушка 200** — держать ликвидной на срочную войну/наёмников.
4. **Постройки** — из «Развития» (20% потока) + излишек выше подушки.
5. **Остаток** — копится «на войну» (до потолка 5000), в мире простаивает.

---

## 3. Пошаговый пример: граф 100 золота, +10/мес → 600 латников и постройки

**Цель:** набрать **полный стек 600 латников** (2 полка тяжёлой пехоты по 300)
и строить здания **+0.5 золота, цена 100** каждое.

**Правила примера (модель):**
- Военная казна = **50** (держим всегда).
- Подушка = **200** (держим перед стройкой) → строим, только когда в казне
  остаётся ≥ 50+200 = **250**.
- Латники (военное) покупаем, держа ≥ 50: найм при казне ≥ `50+90 = 140`.
- Тяжёлая пехота: найм **90**, содержание **0.4/мес** за полк (мирное).
- Здание: цена **100**, даёт **+0.5/мес** к доходу.
- Войны нет.

| Мес | Казна | Доход | Действие | Армия / Здания |
|---|---|---|---|---|
| 0 | 100 | 10.0 | копит (нет 140 на полк) | 0 / 0 |
| 4 | 140 | 10.0 | **найм полк #1 −90** → казна 50 | 300 латн. / 0 |
| 5 | 59.6 | 9.6 | содержание −0.4 → доход 9.6; копит | 300 / 0 |
| 14 | 146 | 9.6 | **найм полк #2 −90** → казна 56 | **600 латн.** ✅ / 0 |
| 15 | 65 | 9.2 | содержание −0.8 всего; копит к 350 | 600 / 0 |
| ~46 | 350 | 9.2 | **постройка #1 −100** → казна 250 | 600 / 1 |
| 47 | 260 | 9.7 | доход +0.5 → 9.7; копит | 600 / 1 |
| ~57 | 357 | 9.7 | **постройка #2 −100** → казна 257 | 600 / 2 |
| ~66 | 350 | 10.2 | **постройка #3 −100** | 600 / 3 |
| ~75 | 350 | 10.7 | **постройка #4 −100** | 600 / 4 |
| ~84 | 350 | 11.2 | **постройка #5 −100** | 600 / 5 |

**Что видно:**
- **Армия (600 латников) набирается быстро** — к ~14-му месяцу (латники дёшевы,
  идут из «военных» 80% потока).
- **Постройки стартуют поздно — только к ~4-му году** (мес. 46), потому что граф
  сначала держит казну 50 + подушку 200 = **250 простаивающих**, и строит лишь из
  излишка сверх этого.
- Дальше **снежок**: каждое здание +0.5 к доходу → следующий интервал чуть короче
  (мес. 46 → 57 → 66 → 75 → 84). Чем больше построек, тем быстрее копится на
  следующую. Но стартовый разгон **мучительно медленный** из-за подушки и того,
  что на развитие идёт лишь ~20% дохода.

**Если бы это был король (+100/мес):** «Война-сейчас» быстро упёрлась бы в потолок
5000, излишек **перелился бы в Развитие**, и постройки шли бы пачками с первых лет
— вот почему крупные сноуболят, а граф нет.

---

## 4. Что тут точно, а что — допущение модели

**Точно из файлов:** все ключи и значения в разделе 1 (казна, 20/80, подушка 200,
потолок 5000, латники 15/40/60% дохода, цены латников, рекомендованное число,
размеры полков, `0.003`/солдат, роспуск 20).

**Допущения модели (в файлах прямо не расписано, проверяется в observer-игре):**
- Точная разводка «найм латников из военных 80% / постройки из развития 20%».
- Точные сроки в месяцах (зависят от реальной разводки и цены зданий).
- Цена здания +0.5/100 — взята из условия задачи (реальные — в `common/buildings/*`).
- Применение `GOLD_COST_PER_SOLDIER` к поднятым левиям (читается из брифа
  «за каждого солдата»; подтвердить наблюдением).

Чтобы заменить допущения точными числами — пришли `common/men_at_arms/*` (реальные
размеры/содержание полков по типам) и `common/buildings/*` (цены и доход зданий).
